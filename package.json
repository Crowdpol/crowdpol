{
  "name": "CommonDemocracy",
  "private": true,
  "scripts": {
    "prod": "meteor --settings=settings.json",
    "start": "meteor --settings=development.settings.json",
    "test": "TEST_WATCH=1 meteor test --driver-package meteortesting:mocha --port 8000"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "bcrypt": "^1.0.3",
    "core-js": "^2.5.1",
    "faker": "^4.1.0",
    "json-schema-faker": "^0.4.6",
    "meteor-node-stubs": "~0.2.4",
    "quill": "^1.3.4",
    "simpl-schema": "^0.3.2",
    "taggle": "^1.12.0"
  }
}
